#include "main.h"
/**
 * results - checks for checks for a digit (0 through 9).
 * @a: a - Variable
 * @b: b - variable
 * Return: Always 0.
 */
int results(int a, int b)
{
int results;

results = a * b;

return (results);
}

